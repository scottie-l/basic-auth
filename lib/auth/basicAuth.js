'use strict';

// const base64 = require('base-64');


// let basicHeaderParts = req.headers.authorization.split(' ');  // ['Basic', 'sdkjdsljd='] - Turn that string into an array by splitting on ' '
// let encodedString = basicHeaderParts.pop();  // sdkjdsljd= - Pop off the last value
// let decodedString = base64.decode(encodedString); // "username:password" - Decode that encoded string so it returns to user:pass
// let [username, password] = decodedString.split(':'); // username, password - Pull username and password from that array
// console.log(`User name: ${username} and Password: ${password}`);

// module.exports = basicAuth;
